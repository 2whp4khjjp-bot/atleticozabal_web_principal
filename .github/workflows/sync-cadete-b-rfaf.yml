name: Sincronizar Cadete B · RFAF
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'scripts/sync-cadete-b-rfaf.js'
      - 'scripts/render-cadete-b-ics.js'
      - 'scripts/validate-cadete-b.js'
      - '.github/workflows/sync-cadete-b-rfaf.yml'
  schedule:
    - cron: '0 8,9 * * *'
permissions:
  contents: write
concurrency:
  group: sync-cadete-b-rfaf
  cancel-in-progress: true
jobs:
  browser:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Comprobar hora peninsular
        id: window
        run: echo "run=$([[ '${{ github.event_name }}' != 'schedule' ]] || TZ=Europe/Madrid date +%H | grep -qx 10 && echo true || echo false)" >> "$GITHUB_OUTPUT"
      - uses: actions/checkout@v4
        if: steps.window.outputs.run == 'true'
      - name: Instalar Playwright y Chromium
        if: steps.window.outputs.run == 'true'
        run: |
          npm install --no-save --package-lock=false playwright@1.63.0
          npx playwright install --with-deps chromium
      - name: Consultar calendario y clasificación
        if: steps.window.outputs.run == 'true'
        run: node scripts/sync-cadete-b-rfaf.js
      - name: Generar calendario de suscripción
        if: steps.window.outputs.run == 'true'
        run: node scripts/render-cadete-b-ics.js
      - name: Validar página y suscripción
        if: steps.window.outputs.run == 'true'
        run: node scripts/validate-cadete-b.js
      - name: Publicar cambios oficiales
        if: steps.window.outputs.run == 'true'
        run: |
          git config user.name "Atlético Zabal · Actualizador"
          git config user.email "actions@github.com"
          git add data/cadete-b-rfaf.json calendario-cadete-b.ics
          if git diff --cached --quiet; then
            echo "No hay cambios oficiales que publicar"
            exit 0
          fi
          git commit -m "Actualiza partidos oficiales Cadete B"
          bash scripts/push-calendar-update.sh
